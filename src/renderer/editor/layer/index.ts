import {ShapeLayer} from "./shape-layer";
import {FrameLayer} from "./frame-layer";
import {TextLayer} from "./text-layer";

export declare type Layer = FrameLayer | ShapeLayer | TextLayer;

export {
    FrameLayer,
    ShapeLayer,
    TextLayer
}
